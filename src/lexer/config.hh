#ifndef __CONFIG_H__
#define __CONFIG_H__

// we neeed a few things
// an enum class to store the tokens itself
// along with a mapping that contains each enum value as a string form along
// with a map of its inverse then we need 2 overloaded functions that can
// do the inverse of each

#endif // __CONFIG_H__